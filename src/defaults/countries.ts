export const SUPPORTED_COUNTRIES: { [key: string]: string } = {
    'ES': 'Spain',
    'MX': 'Mexico',
    'AR': 'Argentina',
}

export const COUNTRY_CODES: Array<string> = Object.keys(SUPPORTED_COUNTRIES);
